# ==============================================================================
# Face Recognition Attendance System — Full Requirements Reference
# ==============================================================================
# This file documents the complete dependency set with explanations.
# The pinned production requirements are in backend/requirements.txt.
#
# Install everything:
#   pip install -r requirements.txt
#
# For GPU-accelerated PyTorch (CUDA 12.1), replace the torch line with:
#   pip install torch==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121
# ==============================================================================

# ------------------------------------------------------------------------------
# Web Framework
# ------------------------------------------------------------------------------
fastapi==0.115.6           # Async Python web framework — powers all REST + WebSocket endpoints
uvicorn[standard]==0.32.1  # ASGI server with websockets and HTTP/1.1 support

# ------------------------------------------------------------------------------
# Database
# ------------------------------------------------------------------------------
sqlalchemy==2.0.36         # ORM — defines Person, Attendance, Image table schemas
aiosqlite==0.20.0          # Async SQLite driver used by SQLAlchemy 2.0 async engine

# ------------------------------------------------------------------------------
# Data Validation & Configuration
# ------------------------------------------------------------------------------
pydantic==2.10.3           # Request/response schema validation (v2 — model_validator, etc.)
pydantic-settings==2.6.1   # .env file loading into typed Settings class
email-validator==2.2.0     # Required by FastAPI for EmailStr fields

# ------------------------------------------------------------------------------
# Authentication & Security
# ------------------------------------------------------------------------------
python-jose[cryptography]==3.3.0  # JWT encode/decode with RS256/HS256
passlib[bcrypt]==1.7.4            # Password hashing — bcrypt scheme
python-multipart==0.0.19          # multipart/form-data parsing (file uploads)

# ------------------------------------------------------------------------------
# Deep Learning — PyTorch
# ------------------------------------------------------------------------------
torch==2.5.1               # PyTorch — tensor ops, CUDA support, cosine similarity search
                           # GPU install: torch==2.5.1+cu121 from pytorch.org/whl/cu121

# ------------------------------------------------------------------------------
# Face Recognition & Computer Vision
# ------------------------------------------------------------------------------
insightface==0.7.3         # Face detection (RetinaFace) + embedding extraction (ArcFace)
                           # Downloads buffalo_l model (~300 MB) on first run
opencv-python==4.10.0.84   # Frame capture from webcam/file, image preprocessing
pillow==11.0.0             # PIL — image open/save/resize operations

# ------------------------------------------------------------------------------
# Vector Search
# ------------------------------------------------------------------------------
faiss-cpu==1.9.0           # Facebook FAISS — IndexFlatIP for cosine-similarity search
                           # GPU: install faiss-gpu instead (requires CUDA toolkit)

# ------------------------------------------------------------------------------
# Numerical Computing
# ------------------------------------------------------------------------------
numpy==2.1.3               # N-dimensional array ops — embedding vectors, distance calcs

# ------------------------------------------------------------------------------
# Data Analysis & Visualisation (dev / notebook use)
# ------------------------------------------------------------------------------
pandas>=2.2.0              # DataFrame ops — useful for attendance analytics scripts
matplotlib>=3.9.0          # Plotting — loss curves, confusion matrices in notebooks
scikit-learn>=1.5.0        # metrics: accuracy_score, confusion_matrix, roc_curve
seaborn>=0.13.0            # Higher-level matplotlib — heatmaps, pair plots

# ------------------------------------------------------------------------------
# Jupyter Notebooks (development only)
# ------------------------------------------------------------------------------
jupyter>=1.1.0             # Jupyter classic notebook server
jupyterlab>=4.2.0          # JupyterLab IDE
ipywidgets>=8.1.0          # Interactive widgets in notebooks
nbstripout>=0.7.1          # Strip notebook output cells before git commit

# ------------------------------------------------------------------------------
# ML Utilities
# ------------------------------------------------------------------------------
tqdm>=4.67.0               # Progress bars for training loops and data processing
python-dotenv>=1.0.0       # Load .env files in standalone scripts

# ------------------------------------------------------------------------------
# Experiment Tracking (optional — uncomment to enable)
# ------------------------------------------------------------------------------
# mlflow>=2.15.0           # Experiment tracking, model registry
# wandb>=0.17.0            # Weights & Biases tracking
# tensorboard>=2.17.0      # TensorBoard loss/metric visualisation

# ------------------------------------------------------------------------------
# Testing
# ------------------------------------------------------------------------------
pytest==8.3.4              # Test runner
pytest-asyncio==0.24.0     # Async test support for FastAPI/SQLAlchemy async code
httpx==0.28.1              # Async HTTP client used in FastAPI TestClient

# ------------------------------------------------------------------------------
# Code Quality (development only)
# ------------------------------------------------------------------------------
ruff>=0.6.0                # Fast Python linter (replaces flake8, isort, pyupgrade)
black>=24.0.0              # Opinionated code formatter
mypy>=1.11.0               # Static type checker
pre-commit>=3.8.0          # Git pre-commit hooks to enforce linting before push
